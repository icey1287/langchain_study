# 优化方向

## 目标
- 优化上下文工程
- 优化记忆系统

## 已完成的改动

### 1. 环境配置集中化管理
在 `backend/config.py` 中集中管理所有环境变量，从 `.env` 文件读取（不读取系统环境变量），这样子方便用户配置。

**涉及文件：**
- `backend/agent.py` - 删除独立的配置加载
- `backend/embedding.py` - 删除独立的配置加载
- `backend/milvus_client.py` - 删除独立的配置加载
- `backend/rag_utils.py` - 删除独立的配置加载
- `backend/rag_pipeline.py` - 删除独立的配置加载
- `backend/tools.py` - 删除独立的配置加载

### 2. API 密钥更名
多增加了很多模型提供商，**可以在env中配置**

### 3. 模型配置
统一使用硅基流动的模型：
- 主模型：`Qwen/Qwen3.5-122B-A10B`
- 嵌入模型：`Qwen/Qwen3-Embedding-4B`
- 重排序模型：`Qwen/Qwen3-Reranker-4B`（注：存在 404 问题待排查）
- 评分模型：`Qwen/Qwen3.5-122B-A10B`

### 4. 修复的问题
- 创建 `backend/__init__.py` 解决模块导入问题
- 修复 `sys.path` 路径问题
- 修复重复前缀导入错误（如 `SILICONFLOW_SILICONFLOW_API_KEY`）

### 5. 新增配置示例文件
创建 `.env.example` 模板文件供用户参考配置。